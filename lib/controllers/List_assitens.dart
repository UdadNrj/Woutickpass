import 'package:flutter/material.dart';

class ListaAsistentesScreen extends StatelessWidget {
  const ListaAsistentesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
