import 'package:flutter/material.dart';

class ConfiguracionEntradasScreen extends StatelessWidget {
  const ConfiguracionEntradasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
